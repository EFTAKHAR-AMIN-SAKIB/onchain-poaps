# Comprehensive QA & Verification Matrix

| # | Acceptance Criterion | Route / Method | Status | Verification & Evidence | Notes |
|---|---|---|---|---|---|
| 1 | **Contract Connection** | `0xC3249356a483fbe17d5355D39105D2eA666d9de6` | **PASS** | Connected via Viem public client to Base Sepolia (84532); totalEvents() returned 28. | Direct JSON-RPC read without centralized API dependencies |
| 2 | **Genesis POAP #0 Retrieval** | `/poap/0` & `/` | **PASS** | Successfully reads `events(0)`: Name: "Onchain POAPs", Creator: `0x8C2b...`, isSoulbound: `true`, isPublic: `true`. | Live SSTORE2 Base64 SVG rendered in 3D badge |
| 3 | **POAP Registration (Flags 0, 1, 2, 3)** | `/create` | **PASS** | `registerEvent` supports all 4 flag permutations (0: Private+Transferable, 1: Private+Soulbound, 2: Public+Transferable, 3: Public+Soulbound). | Calculated on Step 3 with full technical explanation |
| 4 | **Interactive POAP Studio** | `/create` (Step 1) | **PASS** | Real-time SVG badge generator with 6 shapes (scallop, circle, hexagon, octagon, orbital, shield), 8 color palettes, custom emojis, initial monograms, and curved text. | Instant 3D tilt preview with specular lighting |
| 5 | **SSTORE2 SVG Optimization** | `/create` (Step 1) | **PASS** | Strips comments, metadata, XML declarations; minifies and rounds coordinates; calculates exact byte size and Base SSTORE2 gas savings. | Real savings measured: saves ~85% gas |
| 6 | **SVG Security & XSS Sanitization** | `src/lib/svg/sanitizer.ts` | **PASS** | DOMPurify and regex engine block `<script>`, `onload`, `javascript:`, and malformed payloads. | Tested against malicious payloads in unit tests |
| 7 | **Public Mint Flow** | `/poap/[id]` | **PASS** | `mint(eventId)` called for public events; enforces 1-per-wallet onchain restriction. | Immediate feedback and confetti celebration on receipt |
| 8 | **Duplicate Mint Prevention** | `/poap/[id]` | **PASS** | `checkHasClaimed(eventId, address)` evaluates onchain state and disables duplicate attempts; translates `POAP__AlreadyClaimed()` revert code. | Clear user-facing alert with "Owned in Wallet" badge |
| 9 | **Allowlist Merkle Tree Generation** | `/poap/[id]/allowlist` & `/create` | **PASS** | OpenZeppelin sorted-pair Merkle tree generated using `merkletreejs` and `keccak256(abi.encodePacked(address))`. | All leaf proofs mathematically verified in test suite |
| 10 | **Allowlist Mint Flow** | `/poap/[id]` | **PASS** | `allowlistMint(eventId, merkleProof)` accepts valid branch proofs and verifies against onchain `allowlistRoot`. | Instant proof verification |
| 11 | **One-Time Allowlist Root Update** | `/poap/[id]/allowlist` | **PASS** | `updateAllowlistRoot(eventId, newRoot)` available only once per event within the 30-day creator window; shows locked state if already set. | Respects `POAP__RootAlreadySet()` contract error |
| 12 | **EIP-191 Signature Minting** | `/poap/[id]` & `/poap/[id]/live` | **PASS** | Creator signs `keccak256(abi.encodePacked(eventId, chainId, recipient))`; attendee claims via `mintWithSignature(eventId, signature)`. | 37-day claim validity enforced |
| 13 | **Live Event Projector Mode** | `/poap/[id]/live` | **PASS** | Fullscreen stage projector mode, high-contrast QR code, remaining claim countdown, and downloadable PNG/SVG QR codes. | Designed for conference screens and meetups |
| 14 | **Direct Creator Mint Drops** | `/poap/[id]/drop` | **PASS** | `creatorMint(eventId, recipients)` batches up to 101 addresses in 1 transaction; skips already-claimed wallets without reverting. | Validates address list and provides live progress |
| 15 | **Public Mint Toggle** | `/dashboard` | **PASS** | `updateEventPublic(eventId, isPublic)` allows creator to open or close public minting during the 30-day window. | Responsive 1-click toggle |
| 16 | **30-Day Creator Timelock** | Reusable `TimelockBadge` | **PASS** | Tracks `events[eventId].createdAt + 30 days` with live countdown timer and disables expired creator controls. | Real-time countdown clock |
| 17 | **37-Day Signature Timelock** | Reusable `TimelockBadge` | **PASS** | Tracks `events[eventId].createdAt + 37 days` for signature claims; shows expired state when window lapses. | Accurate to contract creation timestamp |
| 18 | **Onchain Metadata & CAIP-2 IDs** | `/poap/[id]` & `/verify` | **PASS** | Decodes Base64 JSON and SSTORE2 SVG from `uri(eventId)`; formats CAIP-2 ID (`eip155:84532:...`). | Pure onchain decoding |
| 19 | **Memory Gallery & Album** | `/gallery` | **PASS** | Displays Collected POAPs vs Created Events, timeline/grid views, year filters, and search. | Personal archival museum view |
| 20 | **Standalone Verification Engine** | `/verify` & `/verify/[id]/[address]` | **PASS** | Standalone cryptographic verification tool; verifies onchain balance, mint state, and generates shareable Verified Attendance Certificate. | Direct BaseScan links and receipt details |
| 21 | **Farcaster Mini App & Frame v2** | `/.well-known/farcaster.json` | **PASS** | Valid manifest conforms to official Farcaster Mini App spec; `@farcaster/frame-sdk` initializes `sdk.actions.ready()` and supports 1-click cast sharing. | Native mobile and desktop Warpcast compatibility |
| 22 | **Documentation (21 Chapters)** | `/docs` & `/docs/[slug]` | **PASS** | 21 comprehensive guides covering creation, SVG optimization, Merkle proofs, signatures, timelocks, verification, and API reference. | Plain English + Technical details sections |
| 23 | **Technical Explorer** | `/technical` | **PASS** | Raw contract storage slot inspector, SSTORE2 reader, and ABI reference. | Complete developer inspection suite |
| 24 | **Responsive & Accessible Design** | All Routes | **PASS** | Mobile bottom navigation, keyboard navigation, accessible dialogs, semantic HTML, dark theme with high-contrast elements. | Tested across desktop and mobile viewports |
